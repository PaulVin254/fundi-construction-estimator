# Import the `fundi_estimator` submodule from the current `agents` package.
# This triggers the execution of `agents/fundi_estimator/__init__.py`,   
# which in turn typically loads or exposes the agent defined in `agent.py`.
from . import agent